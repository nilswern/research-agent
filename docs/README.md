# Docs

Screenshots referenced by the main README. Both are commented out there until
the files exist; uncomment the line once you add one.

| File | What it should show |
| --- | --- |
| `ui.png` | The browser UI mid-run: plan and activity log on the left, report streaming in on the right |
| `trace.png` | One run in LangSmith, if you enable tracing |

Keep them under a few hundred KB - a wide PNG at 1600px is plenty.
